((function () {
    'use strict';

    Cobweb.prototype.modules.add('scene', function (instance) {
        var root = tree({
            
        });

        var scene = {
            root: root
        };

        instance.scene = scene;
    });
})());
